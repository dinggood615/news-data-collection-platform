services:
  news-platform:
    build: .
    container_name: news-platform
    restart: unless-stopped
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - news_platform_data:/data
    extra_hosts:
      - "host.docker.internal:host-gateway"
volumes:
  news_platform_data:
